import React from 'react'

import UserContactsList from "../features/user/components/UserContactsList";

const UserContactsListPage = () => {
    return(
        <UserContactsList/>
    )
}

export default UserContactsListPage